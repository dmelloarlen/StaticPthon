The dataset used for the assignment was of covid-19.it contains the world wide data of patients suffrirng from covid-19 country wise.

A python code can be "run" just by writing  "& <path>" in the terminal and in same directory in which the ".py" file is present.a "streamlit" file can be run by writing "streamlit run <filename>" command on terminal.

"streamlit" library was imported and was used for creating "dashboard" and "matplotlib.pyplot" library was user for plotting the charts which where later displayed on the "streamlit" app.the "dashboard" is user interective and the user can choose the perticular factor for which the chart will be displayed.

For exception handling first the exceptions were declared in "exception2.py" an where raised whereaver there is an chance of a specific exception.